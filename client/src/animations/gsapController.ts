import { gsap } from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';

gsap.registerPlugin(ScrollTrigger);

export const initGSAPAnimations = () => {
  // GSAP animation foundation stub - ready for Phase 05
  return gsap;
};
